# 这是一个环境变量示例文件。
# 请将此文件复制为 .env 并填入您自己的值。
# .env 文件通常不应提交到版本控制系统 (git) 中。

# --- 核心配置 ---
# 设置一个安全的管理员后台登录密码
ADMIN_PASSWORD=qwq


# --- Vercel 部署专用 (可选) ---
# 如果您计划部署到 Vercel 并使用 Vercel KV 作为数据库，
# Vercel 会自动提供这些环境变量，您无需在此手动填写。
# 此处列出仅为说明文档用途。
# KV_URL=""
# KV_REST_API_URL=""
# KV_REST_API_TOKEN=""
# KV_REST_API_READ_ONLY_TOKEN=""